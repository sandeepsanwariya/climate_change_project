import React from "react"

class SiteMenu extends React.Component {
  render() {
    return (
        <nav>
          <ul>
            {/* {this.props.menu.map(menuItem => (
            <li key={SiteMenu.id}>{menuItem.title}</li>
            ))} */}
          </ul>
        </nav>
    )
  }
}
export default SiteMenu